#!/bin/sh

# Enter the {{ podName }}-{{ anImage }} container in the {{ podName }} pod

if podman container exists {{ podName }}-{{ anImage }} ; then
  podman exec -it --env ENV=/root/.bashrc {{ podName }}-{{ anImage }} /bin/sh
fi
