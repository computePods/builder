# This cpf YAML file describes how to build the cpChef CEKit module.

# The important thing about this description is that the Python modules 
# require the two stage build. In this YAML file we describe the build 
# stage modules which pair with this run stage module. 

buildModule: cpChefBuild-apk

basedOn: alpine

packagesManager: apk
  
