# This CEKit module describes how to build the syncThing computePud container

# This module:
# - assumes a Debian based container,

# see: https://github.com/nats-io/nats-docker/tree/master/2.2.1

schema_version: 1

name: syncThingServerBuild
version: 1.0
description: Builds the SyncThing file synchronization service
from: golang:alpine

execute:
  - script: buildSyncThing.sh

packages:
  manager: apk
  install:
    - git
