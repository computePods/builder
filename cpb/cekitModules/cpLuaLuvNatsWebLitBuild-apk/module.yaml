# This CEKit module describes how to build the computePods MajorDomo tool 
# on Alpine. 

# This module:
# - assumes an Alpine (apk) based container,

schema_version: 1

name: cpLuaLuvNatsWebLitBuild-apk
version: 1.0
description: Builds the Alpine version of Lua, Luv, Luv-Nats, and WebLit base image
from: alpine

execute:
  - script: buildCPLuaLuvNatsWebLit.sh

packages:
  manager: apk
  install:
    - git
    - gcc
    - make
    - cmake
    - musl-dev
    - pkgconf-dev
    - openssl-dev
    - wget
    - ca-certificates

artifacts:
  - name: lua.pc
    path: lua.pc
